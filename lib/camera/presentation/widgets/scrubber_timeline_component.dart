import 'package:flutter/material.dart';

class ScrubberTimelineComponent extends StatelessWidget {
  const ScrubberTimelineComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 80,
      color: const Color.fromARGB(122, 111, 133, 139)
    );
  }
}