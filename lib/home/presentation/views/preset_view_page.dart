import 'package:flutter/material.dart';

class PresetViewsPage extends StatelessWidget {
  const PresetViewsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Preset Views Content", style: TextStyle(color: Colors.white, fontSize: 20)),
    );
  }
}
