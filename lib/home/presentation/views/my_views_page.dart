import 'package:flutter/material.dart';

class MyViewsPage extends StatelessWidget {
  const MyViewsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("My Views Content", style: TextStyle(color: Colors.white, fontSize: 20)),
    );
  }
}